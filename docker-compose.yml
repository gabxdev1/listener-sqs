services:

  api:
    image: gabxdevv/listener-sqs:0.0.7
    environment:
      - JAVA_TOOL_OPTIONS=-javaagent:/app/dd-java-agent.jar
      - DD_AGENT_HOST=host.docker.internal
      - AWS_ACCESS_KEY_ID=
      - AWS_SECRET_ACCESS_KEY=
      - AWS_REGION=sa-east-1
      - AWS_DEFAULT_REGION=sa-east-1
    ports:
      - "8080:8080"



